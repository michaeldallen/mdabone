{
  "image": "mcr.microsoft.com/devcontainers/universal:6",
  "features": {
    "ghcr.io/devcontainers/features/git-lfs:1": {
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.14",
      "installTools": false
    },
    "./features/mdabone-support": {
      "pythonPackages": "numpy trimesh pytest"
    }
  },

  // Triggers environment detection, then updates submodules after git URL rules are set
  "postCreateCommand": "./.devcontainer/setup-git.sh && git submodule sync --recursive && git submodule update --init --recursive",

  // Customizes your VS Code terminal settings inside the Codespace
  "customizations": {
    "codespaces": {
      "repositories": {
        "michaeldallen/mda": {
          "permissions": {
            "contents": "write"
          }
        }
      }
    },
    "vscode": {
      "extensions": [
        "github.vscode-github-actions",
        "ms-azuretools.vscode-containers",
        "ms-vscode-remote.remote-containers",
        "ms-vscode.makefile-tools"
      ],
      "settings": {
        "git.autofetch": true,
        "git.detectSubmodules": true,
        "git.submoduleLimit": 10,
        // Ensures your terminal looks for binaries in local user folders
        "terminal.integrated.env.linux": {
          "PATH": "${env:PATH}:${userHome}/.local/bin"
        }
      }
    }
  }
}